import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.dwaps.cocojaco',
  appName: 'my-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
